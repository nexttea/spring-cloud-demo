server:
    port: 8802
spring:
    application:
        name: client
eureka:
    instance:
      prefer-ip-address: true
    server:
        port: 8701
        hostname: localhost
    client:
        service-url:
            defaultZone: http://${eureka.server.hostname}:${eureka.server.port}/eureka/